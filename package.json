{
  "name": "dsh-compat-suite",
  "version": "0.0.0",
  "private": true,
  "description": "A compatibility discovery, validation, and reporting suite for DeepSeek Harness hosts and plugins.",
  "license": "MIT",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "verify:g0": "node scripts/verify-repository.mjs",
    "verify": "pnpm run verify:g0 && pnpm run build && pnpm run typecheck && pnpm run test",
    "changeset": "changeset",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "3.0.2"
  }
}
