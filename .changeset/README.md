# Changesets

Add one Markdown changeset for every user-visible package change. The
repository is lockstep-versioned: changesets for `@miguel_tu/core`,
`@miguel_tu/doctor`, and `@miguel_tu/plugin` are released together.

Do not add release credentials, registry tokens, generated tarballs, or real
runtime data here.
